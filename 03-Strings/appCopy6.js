const producto = 'Monitor 20 pulgadas';

// Repeat permite repetir una cadena de texto
const texto =  ` en promoción`.repeat(2);

console.log(texto);
console.log(`${producto} ${texto} !!!`);

// Split = divir un string
const actividad = "Estoy aprendiendo JavaScript";
console.log(actividad.split(''));

const hobbies = 'Leer, caminar, escuchar musica, aprender a programar';
console.log(hobbies.split(";"));

const tweet = 'Aprendiendo a programar JavaScript';
console.log(tweet.split('#'));