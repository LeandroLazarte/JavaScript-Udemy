// Los arreglos comienza con indice 0...
const numeros = [10, 20, 30];
const meses = new Array('Enero', 'Febrero', 'Marzo', 'Abril');

console.log(numeros);
console.log(meses);

// Arreglo que contiene datos de todo tipo
const deTodo = ["Hola", 10, true, "Si", null, ("Trabajo como programador"),[1,2,3]];
console.log(deTodo);
